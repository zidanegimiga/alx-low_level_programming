#include "holberton.h"
#include <stdio.h>

/**
 * main - a program that prints its own name
 * @argc: argument that counts the commands passed to the program
 * @argv: argument vector
 * Return: An integer
 */
int main (int argc, char *argv[])
{
	(void)argsc;
	printf("%s\n", argv[0]);
	return(0);
}
