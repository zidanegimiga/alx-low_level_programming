#include <stdio.h>

/**
 * main - prints arguments passed to it by the cmd
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: int
 */
int main(int argc, char *argv[])
{
	int count = 0;

	while (count < argc)
	{
		printf("%s\n", argv(count);
		count++;
	}
	return (0);
}
