#include "holberton.h"

/**
 * add - adds two integers and returns the sum
 * @n: added to m
 * @m: added to n
 *
 * Return: returns the sum
 */
int add(int n, int m)
{	int sum;

	sum = m + n;
	return (sum);
}
